# After running /quiz-add History medium

Snapshot of `questions.js` right after the book's `/quiz-add History medium` exercise.

The command appends one new medium-difficulty History question ("In what year did the French Revolution begin?", id 41) to the base 40 questions, for a total of 41. The project root keeps the base 40-question set.

Files:
- `questions.js` - the question data with the newly added question
